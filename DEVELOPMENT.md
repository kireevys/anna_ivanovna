# Anna Ivanovna - Руководство для разработчиков

[![Rust](https://github.com/kireevys/anna_ivanovna/actions/workflows/rust.yml/badge.svg)](https://github.com/kireevys/anna_ivanovna/actions/workflows/rust.yml)

Техническая документация для разработчиков, желающих внести вклад в проект или понять его архитектуру.

## 📁 Структура проекта

```
anna_ivanovna/
├── src/
│   ├── main.rs          # Точка входа
│   ├── lib.rs           # Основные модули
│   ├── cli.rs           # CLI интерфейс
│   ├── planning.rs      # Логика планирования бюджета
│   ├── finance.rs       # Финансовые типы (Money, Percentage)
│   ├── distribute.rs    # Распределение доходов
│   └── storage.rs       # Работа с YAML файлами
├── src/test_storage/    # Тестовые данные
├── benches/             # Бенчмарки
└── docs/                # Документация
```

## 🏗️ Архитектура

### Основные модули

#### 1. Планирование (`planning.rs`)

Содержит логику создания и валидации бюджетного плана:

- **`Plan`** - основной план бюджета с источниками доходов и расходами
- **`Draft`** - черновик плана для пошагового создания
- **`IncomeSource`** - источник дохода (название + ожидаемая сумма)
- **`Expense`** - статья расхода (название + значение в рублях или процентах)
- **`ExpenseValue`** - enum для значений расходов (RATE/MONEY)

#### 2. Финансы (`finance.rs`)

Финансовые типы данных:

- **`Money`** - денежная сумма с валютой (поддерживает RUB, USD)
- **`Percentage`** - процентное значение с математическими операциями
- **`Currency`** - перечисление валют

#### 3. Распределение (`distribute.rs`)

Логика распределения доходов:

- **`Income`** - полученный доход с источником и датой
- **`Distribute`** - результат распределения с суммами по статьям
- **`distribute()`** - основная функция распределения

#### 4. Хранилище (`storage.rs`)

Работа с YAML файлами:

- **`plan_from_yaml()`** - чтение плана из YAML
- **`distribute_to_yaml()`** - сохранение результатов в YAML

#### 5. CLI (`cli.rs`)

Интерфейс командной строки:

- **`Cli`** - основная структура CLI
- **`Commands`** - подкоманды
- **`run()`** - точка входа CLI

## 🔧 Разработка

### Требования

- Rust 1.70+
- Cargo

### Установка зависимостей

```bash
cargo build
```

### Запуск тестов

```bash
# Все тесты
cargo test

# Конкретный модуль
cargo test planning

# С покрытием (требует grcov)
cargo test --coverage
```

### Бенчмарки

```bash
cargo bench
```

### Линтинг

```bash
# Clippy
cargo clippy --all-targets --all-features -- -W clippy::style -W clippy::perf -D warnings

# Форматирование
cargo fmt
```

## 📊 Формат данных

### YAML структура плана

```yaml
version: "1.0"
plan:
  incomes:
    - source: "Название источника"
      value: "₽50000"  # или "50000₽"
  expenses:
    - name: "Название статьи"
      value: "30%"     # процент от дохода
    - name: "Фиксированная статья"
      value: "₽5000"   # фиксированная сумма
```

### Поддерживаемые форматы

- **Проценты:** `30%`, `15.5%`
- **Деньги:** `₽5000`, `5000₽`, `100.50₽`

## 🧪 Тестирование

### Структура тестов

- **Модульные тесты** - в каждом файле с `#[cfg(test)]`
- **Интеграционные тесты** - в `tests/` (если есть)
- **Бенчмарки** - в `benches/`

### Примеры тестов

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_money_creation() {
        let money = Money::new_rub(dec!(100.50));
        assert_eq!(money.value, dec!(100.50));
        assert_eq!(money.currency, Currency::RUB);
    }
}
```

## 🔍 Ограничения и особенности

### Технические ограничения

- Поддерживаются только рубли (RUB) и доллары (USD)
- Процентные значения округляются до 2 знаков после запятой
- Сумма всех расходов не должна превышать 100% от дохода
- Один план может содержать несколько источников дохода

### Бизнес-логика

- Фиксированные расходы обрабатываются первыми
- Процентные расходы рассчитываются от оставшейся суммы
- Остаток = доход - все расходы

## 🚀 Сборка и релиз

### Локальная сборка

```bash
# Debug
cargo build

# Release
cargo build --release
```

### Установка

```bash
# Глобальная установка
cargo install --path .

# Проверка версии
anna_ivanovna --version
```

### CI/CD

Проект использует GitHub Actions для автоматизации:

- **`checks.yml`** - проверка кода при PR (тесты, clippy)
- **`release.yml`** - создание релизов при создании тега

#### Автоматическая сборка

При создании тега (релиза) автоматически собираются бинарные файлы для:
- Linux (x86_64)
- macOS (Intel x86_64)
- macOS (Apple Silicon aarch64)
- Windows (x86_64)

Архивы прикрепляются к релизу на GitHub.

#### Создание релиза

```bash
# Создание тега
git tag v0.1.0
git push origin v0.1.0

# Или через GitHub CLI
gh release create v0.1.0 --generate-notes
```

### Релиз

```bash
# Создание релиза
cargo release

# Публикация в crates.io (отключено в Cargo.toml)
cargo publish
```

## 📦 Зависимости

### Основные зависимости

- **`rust_decimal`** - точные десятичные числа
- **`chrono`** - работа с датами
- **`serde`** - сериализация/десериализация
- **`serde_yaml`** - YAML формат
- **`clap`** - CLI парсинг
- **`uuid`** - уникальные идентификаторы

### Dev зависимости

- **`criterion`** - бенчмарки

## 🔧 Конфигурация

### Переменные окружения

- **`HOMEVAR`** - путь к папке с данными (по умолчанию `$HOME`)
- **`.env`** - файл с переменными окружения (опционально)

### Структура папок

```
$HOMEVAR/
└── storage/
    ├── plan.yaml      # План бюджета
    └── incomes/       # Результаты распределения
        ├── 2024-01-15.yaml
        ├── 2024-01-16.yaml
        └── ...
```

## 🤝 Вклад в проект

### Процесс разработки

1. **Форкните репозиторий**
2. **Создайте ветку** для новой функции:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. **Внесите изменения** и добавьте тесты
4. **Зафиксируйте изменения**:
   ```bash
   git commit -m 'Add amazing feature'
   ```
5. **Отправьте в ветку**:
   ```bash
   git push origin feature/amazing-feature
   ```
6. **Откройте Pull Request**

### Стандарты кода

- **Rustfmt** для форматирования
- **Clippy** для линтинга
- **Тесты** для новой функциональности
- **Документация** для публичных API

### Коммиты

Используйте conventional commits:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `test:` - тесты
- `refactor:` - рефакторинг

## 🐛 Отладка

### Логирование

```bash
# Включить логи
RUST_LOG=debug cargo run -- add-income 50000
```

### Отладка CLI

```bash
# Показать help
cargo run -- --help

# Отладка конкретной команды
cargo run -- add-income 50000
```

## 📚 Дополнительные ресурсы

- [Rust Book](https://doc.rust-lang.org/book/)
- [Cargo Book](https://doc.rust-lang.org/cargo/)
- [Serde Documentation](https://serde.rs/)
- [Clap Documentation](https://docs.rs/clap/)

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE) для деталей.

---

**Для пользователей:** см. [README.md](README.md) 